# CompatGuard (monorepo)

This repository is an expanded CompatGuard project with multiple packages and build wiring. It's a functional developer scaffold (stubs) assembled from the uploaded design doc. See package READMEs for details.
